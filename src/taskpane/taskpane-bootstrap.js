import { createTaskPaneShell } from "./taskpane-shell.js";
import { createTaskPaneViewModel } from "./taskpane-view-model.js";

// Placeholder bootstrap entry for the future task pane page generated by Yeoman.
export function bootstrapTaskPane() {
  return {
    shell: createTaskPaneShell(),
    viewModel: createTaskPaneViewModel(),
    status: "placeholder"
  };
}
